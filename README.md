<h1>Explanation of the project </h1>
<h3>The code sorts an array using a selection‑insertion hybrid method.

It repeatedly finds the smallest element in the unsorted part.

Instead of swapping, it shifts elements and inserts the minimum at the correct position.

The result is a neatly sorted array in ascending order.</h3>
